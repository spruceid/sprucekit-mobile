// Autogenerated from Pigeon (v26.3.4), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.spruceid.sprucekit_mobile

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object MdlPresentationPigeonUtils {

  fun createConnectionError(channelName: String): MdlPresentationFlutterError {
    return MdlPresentationFlutterError("channel-error",  "Unable to establish connection on channel: '$channelName'.", "")  }

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is MdlPresentationFlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class MdlPresentationFlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : RuntimeException()

/**
 * Presentation state for ISO 18013-5 mDL presentation
 *
 * An NFC tap based presentation reports its NFC detail in
 * [MdlPresentationStateUpdate.nfcPhase] and keeps this enum unchanged, so
 * existing exhaustive switches still compile. See [MdlNfcPhase] for the
 * planned merge.
 */
enum class MdlPresentationState(val raw: Int) {
  /** Initial state, not yet started */
  UNINITIALIZED(0),
  /** Waiting for Bluetooth/transport to be ready */
  INITIALIZING(1),
  /** Bluetooth needs to be turned on to continue */
  BLUETOOTH_REQUIRED(2),
  /** Bluetooth authorization required in Settings */
  BLUETOOTH_AUTHORIZATION_REQUIRED(3),
  /** QR code is ready to be displayed */
  ENGAGING_QR_CODE(4),
  /** Reader has connected, waiting for request */
  CONNECTED(5),
  /** Received request from reader, user needs to select fields */
  SELECTING_NAMESPACES(6),
  /** Sending response to reader */
  SENDING_RESPONSE(7),
  /** Successfully sent response */
  SUCCESS(8),
  /** Session timed out */
  TIMEOUT(9),
  /** Reader disconnected unexpectedly */
  READER_DISCONNECTED(10),
  /** An error occurred */
  ERROR(11);

  companion object {
    fun ofRaw(raw: Int): MdlPresentationState? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * Where an NFC tap based presentation is, on top of [MdlPresentationState]
 *
 * Set on Android after [MdlPresentation.initializeNfcPresentation]. On iOS
 * only when the SDK reports `connectingViaNfc`, which the plugin does not
 * reach today. The main state stays within the existing values, so callers
 * that do not know about NFC keep working: `initializing` while the tap is
 * pending and `error` when NFC is turned off.
 *
 * Transitional. The next major release adds `nfcWaitingForTap`,
 * `connectingViaNfc` and `nfcUnavailable` to [MdlPresentationState],
 * removes this enum and the `nfcPhase` field, and reimplements the
 * `MdlPresentationStateUpdate` getters (`isWaitingForNfcTap`,
 * `isConnectingViaNfc`, `isNfcUnavailable`) on the state. Code that uses
 * the getters needs no change at that point.
 */
enum class MdlNfcPhase(val raw: Int) {
  /** The phone answers reader taps. State is `initializing`. */
  WAITING_FOR_TAP(0),
  /**
   * The tap delivered the BLE carrier. Connection in progress. State is
   * `initializing`.
   */
  CONNECTING(1),
  /** NFC was turned off while waiting. State is `error`. Fall back to QR. */
  UNAVAILABLE(2);

  companion object {
    fun ofRaw(raw: Int): MdlNfcPhase? {
      return values().firstOrNull { it.raw == raw }
    }
  }
}

/**
 * Requested item from a namespace
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class MdlNamespaceItem (
  /** Field name */
  val name: String,
  /** Whether the field is required by the reader */
  val intentToRetain: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): MdlNamespaceItem {
      val name = pigeonVar_list[0] as String
      val intentToRetain = pigeonVar_list[1] as Boolean
      return MdlNamespaceItem(name, intentToRetain)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      name,
      intentToRetain,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as MdlNamespaceItem
    return MdlPresentationPigeonUtils.deepEquals(this.name, other.name) && MdlPresentationPigeonUtils.deepEquals(this.intentToRetain, other.intentToRetain)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.name)
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.intentToRetain)
    return result
  }
}

/**
 * Request from reader for a specific namespace
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class MdlNamespaceRequest (
  /** Namespace identifier (e.g., "org.iso.18013.5.1") */
  val namespace: String,
  /** Items requested in this namespace */
  val items: List<MdlNamespaceItem>
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): MdlNamespaceRequest {
      val namespace = pigeonVar_list[0] as String
      val items = pigeonVar_list[1] as List<MdlNamespaceItem>
      return MdlNamespaceRequest(namespace, items)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      namespace,
      items,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as MdlNamespaceRequest
    return MdlPresentationPigeonUtils.deepEquals(this.namespace, other.namespace) && MdlPresentationPigeonUtils.deepEquals(this.items, other.items)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.namespace)
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.items)
    return result
  }
}

/**
 * Items request from reader for a specific docType
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class MdlItemsRequest (
  /** Document type (e.g., "org.iso.18013.5.1.mDL") */
  val docType: String,
  /** Namespaces and their requested items */
  val namespaces: List<MdlNamespaceRequest>
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): MdlItemsRequest {
      val docType = pigeonVar_list[0] as String
      val namespaces = pigeonVar_list[1] as List<MdlNamespaceRequest>
      return MdlItemsRequest(docType, namespaces)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      docType,
      namespaces,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as MdlItemsRequest
    return MdlPresentationPigeonUtils.deepEquals(this.docType, other.docType) && MdlPresentationPigeonUtils.deepEquals(this.namespaces, other.namespaces)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.docType)
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.namespaces)
    return result
  }
}

/**
 * State update event from the presentation session
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class MdlPresentationStateUpdate (
  /** Current state */
  val state: MdlPresentationState,
  /** QR code URI (only set when state is engagingQrCode) */
  val qrCodeUri: String? = null,
  /** Items requests from reader (only set when state is selectingNamespaces) */
  val itemsRequests: List<MdlItemsRequest>? = null,
  /** Error message (only set when state is error) */
  val error: String? = null,
  /**
   * NFC detail for an NFC tap based presentation
   *
   * Transitional, see [MdlNfcPhase]. Read it through the getters.
   */
  val nfcPhase: MdlNfcPhase? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): MdlPresentationStateUpdate {
      val state = pigeonVar_list[0] as MdlPresentationState
      val qrCodeUri = pigeonVar_list[1] as String?
      val itemsRequests = pigeonVar_list[2] as List<MdlItemsRequest>?
      val error = pigeonVar_list[3] as String?
      val nfcPhase = pigeonVar_list[4] as MdlNfcPhase?
      return MdlPresentationStateUpdate(state, qrCodeUri, itemsRequests, error, nfcPhase)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      state,
      qrCodeUri,
      itemsRequests,
      error,
      nfcPhase,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as MdlPresentationStateUpdate
    return MdlPresentationPigeonUtils.deepEquals(this.state, other.state) && MdlPresentationPigeonUtils.deepEquals(this.qrCodeUri, other.qrCodeUri) && MdlPresentationPigeonUtils.deepEquals(this.itemsRequests, other.itemsRequests) && MdlPresentationPigeonUtils.deepEquals(this.error, other.error) && MdlPresentationPigeonUtils.deepEquals(this.nfcPhase, other.nfcPhase)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.state)
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.qrCodeUri)
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.itemsRequests)
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.error)
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.nfcPhase)
    return result
  }
}

/**
 * Result type for presentation operations
 *
 * Generated class from Pigeon that represents data sent in messages.
 * This class should not be extended by any user class outside of the generated file.
 */
sealed class MdlPresentationResult 
/**
 * Operation succeeded
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class MdlPresentationSuccess (
  val message: String? = null
) : MdlPresentationResult()
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): MdlPresentationSuccess {
      val message = pigeonVar_list[0] as String?
      return MdlPresentationSuccess(message)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      message,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as MdlPresentationSuccess
    return MdlPresentationPigeonUtils.deepEquals(this.message, other.message)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.message)
    return result
  }
}

/**
 * Operation failed
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class MdlPresentationError (
  val message: String
) : MdlPresentationResult()
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): MdlPresentationError {
      val message = pigeonVar_list[0] as String
      return MdlPresentationError(message)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      message,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as MdlPresentationError
    return MdlPresentationPigeonUtils.deepEquals(this.message, other.message)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + MdlPresentationPigeonUtils.deepHash(this.message)
    return result
  }
}
private open class MdlPresentationPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          MdlPresentationState.ofRaw(it.toInt())
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as Long?)?.let {
          MdlNfcPhase.ofRaw(it.toInt())
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          MdlNamespaceItem.fromList(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          MdlNamespaceRequest.fromList(it)
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          MdlItemsRequest.fromList(it)
        }
      }
      134.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          MdlPresentationStateUpdate.fromList(it)
        }
      }
      135.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          MdlPresentationSuccess.fromList(it)
        }
      }
      136.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          MdlPresentationError.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is MdlPresentationState -> {
        stream.write(129)
        writeValue(stream, value.raw.toLong())
      }
      is MdlNfcPhase -> {
        stream.write(130)
        writeValue(stream, value.raw.toLong())
      }
      is MdlNamespaceItem -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      is MdlNamespaceRequest -> {
        stream.write(132)
        writeValue(stream, value.toList())
      }
      is MdlItemsRequest -> {
        stream.write(133)
        writeValue(stream, value.toList())
      }
      is MdlPresentationStateUpdate -> {
        stream.write(134)
        writeValue(stream, value.toList())
      }
      is MdlPresentationSuccess -> {
        stream.write(135)
        writeValue(stream, value.toList())
      }
      is MdlPresentationError -> {
        stream.write(136)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/**
 * Callback interface for presentation state updates
 *
 * Generated class from Pigeon that represents Flutter messages that can be called from Kotlin.
 */
class MdlPresentationCallback(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by MdlPresentationCallback. */
    val codec: MessageCodec<Any?> by lazy {
      MdlPresentationPigeonCodec()
    }
  }
  /** Called when the presentation state changes */
  fun onStateChange(updateArg: MdlPresentationStateUpdate, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.sprucekit_mobile.MdlPresentationCallback.onStateChange$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(updateArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(MdlPresentationFlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(MdlPresentationPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
}
/**
 * ISO 18013-5 mDL Presentation API
 *
 * Handles close proximity presentation of mDL credentials via BLE
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface MdlPresentation {
  /**
   * Initialize a QR code based presentation session
   *
   * @param packId The credential pack ID containing the mDL
   * @param credentialId The credential ID of the mDL to present
   * @return Result indicating success or error
   */
  fun initializeQrPresentation(packId: String, credentialId: String, callback: (Result<MdlPresentationResult>) -> Unit)
  /**
   * Whether this device can present over an NFC tap
   *
   * True when the device has an NFC adapter that is turned on and
   * supports host card emulation. Always false on iOS.
   */
  fun isNfcPresentationAvailable(): Boolean
  /**
   * Arm an NFC tap based presentation session
   *
   * The BLE session starts when a reader taps the phone. The engagement
   * uses ISO 18013-5 static handover with BLE central client mode. State
   * updates arrive on [MdlPresentationCallback.onStateChange].
   *
   * @param packId The credential pack ID containing the mDL
   * @param credentialId The credential ID of the mDL to present
   * @return Result indicating success or error
   */
  fun initializeNfcPresentation(packId: String, credentialId: String, callback: (Result<MdlPresentationResult>) -> Unit)
  /**
   * Get the current QR code URI for the presentation
   *
   * @return The QR code URI, or null if not in the correct state
   */
  fun getQrCodeUri(): String?
  /**
   * Get the current presentation state
   *
   * @return Current state update with all relevant data
   */
  fun getCurrentState(): MdlPresentationStateUpdate
  /**
   * Submit the user's selection of namespaces/fields to share
   *
   * The map structure is: docType -> namespace -> list of field names
   *
   * @param selectedNamespaces The selected namespaces and fields
   * @return Result indicating success or error
   */
  fun submitNamespaces(selectedNamespaces: Map<String, Map<String, List<String>>>, callback: (Result<MdlPresentationResult>) -> Unit)
  /** Cancel the current presentation session */
  fun cancel()

  companion object {
    /** The codec used by MdlPresentation. */
    val codec: MessageCodec<Any?> by lazy {
      MdlPresentationPigeonCodec()
    }
    /** Sets up an instance of `MdlPresentation` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: MdlPresentation?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.MdlPresentation.initializeQrPresentation$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val packIdArg = args[0] as String
            val credentialIdArg = args[1] as String
            api.initializeQrPresentation(packIdArg, credentialIdArg) { result: Result<MdlPresentationResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MdlPresentationPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MdlPresentationPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.MdlPresentation.isNfcPresentationAvailable$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.isNfcPresentationAvailable())
            } catch (exception: Throwable) {
              MdlPresentationPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.MdlPresentation.initializeNfcPresentation$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val packIdArg = args[0] as String
            val credentialIdArg = args[1] as String
            api.initializeNfcPresentation(packIdArg, credentialIdArg) { result: Result<MdlPresentationResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MdlPresentationPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MdlPresentationPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.MdlPresentation.getQrCodeUri$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.getQrCodeUri())
            } catch (exception: Throwable) {
              MdlPresentationPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.MdlPresentation.getCurrentState$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.getCurrentState())
            } catch (exception: Throwable) {
              MdlPresentationPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.MdlPresentation.submitNamespaces$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val selectedNamespacesArg = args[0] as Map<String, Map<String, List<String>>>
            api.submitNamespaces(selectedNamespacesArg) { result: Result<MdlPresentationResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(MdlPresentationPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(MdlPresentationPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.MdlPresentation.cancel$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              api.cancel()
              listOf(null)
            } catch (exception: Throwable) {
              MdlPresentationPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
