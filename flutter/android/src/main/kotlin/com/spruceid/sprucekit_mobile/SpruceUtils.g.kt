// Autogenerated from Pigeon (v26.1.4), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.spruceid.sprucekit_mobile

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object SpruceUtilsPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is SpruceUtilsFlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a is ByteArray && b is ByteArray) {
        return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
        return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
        return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
        return a.contentEquals(b)
    }
    if (a is Array<*> && b is Array<*>) {
      return a.size == b.size &&
          a.indices.all{ deepEquals(a[it], b[it]) }
    }
    if (a is List<*> && b is List<*>) {
      return a.size == b.size &&
          a.indices.all{ deepEquals(a[it], b[it]) }
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      return a.size == b.size && a.all {
          (b as Map<Any?, Any?>).contains(it.key) &&
          deepEquals(it.value, b[it.key])
      }
    }
    return a == b
  }
      
}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class SpruceUtilsFlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : Throwable()

/**
 * Result of generating a mock mDL
 *
 * Generated class from Pigeon that represents data sent in messages.
 * This class should not be extended by any user class outside of the generated file.
 */
sealed class GenerateMockMdlResult 
/**
 * Mock mDL generated successfully - stored in a CredentialPack
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class GenerateMockMdlSuccess (
  /** The credential pack ID where the mDL was stored */
  val packId: String,
  /** The credential ID of the generated mDL */
  val credentialId: String,
  /** The raw mSO mDoc as base64 string for storage */
  val rawCredential: String,
  /** The key alias used for signing */
  val keyAlias: String
) : GenerateMockMdlResult()
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): GenerateMockMdlSuccess {
      val packId = pigeonVar_list[0] as String
      val credentialId = pigeonVar_list[1] as String
      val rawCredential = pigeonVar_list[2] as String
      val keyAlias = pigeonVar_list[3] as String
      return GenerateMockMdlSuccess(packId, credentialId, rawCredential, keyAlias)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      packId,
      credentialId,
      rawCredential,
      keyAlias,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other !is GenerateMockMdlSuccess) {
      return false
    }
    if (this === other) {
      return true
    }
    return SpruceUtilsPigeonUtils.deepEquals(toList(), other.toList())  }

  override fun hashCode(): Int = toList().hashCode()
}

/**
 * Mock mDL generation failed
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class GenerateMockMdlError (
  val message: String
) : GenerateMockMdlResult()
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): GenerateMockMdlError {
      val message = pigeonVar_list[0] as String
      return GenerateMockMdlError(message)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      message,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other !is GenerateMockMdlError) {
      return false
    }
    if (this === other) {
      return true
    }
    return SpruceUtilsPigeonUtils.deepEquals(toList(), other.toList())  }

  override fun hashCode(): Int = toList().hashCode()
}
private open class SpruceUtilsPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          GenerateMockMdlSuccess.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          GenerateMockMdlError.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is GenerateMockMdlSuccess -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is GenerateMockMdlError -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/**
 * Utility functions for credential operations
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface SpruceUtils {
  /**
   * Generate a mock mDL for testing
   *
   * Creates a self-signed test mDL credential and stores it in a new
   * CredentialPack. The returned packId and credentialId can be used with
   * MdlPresentation.initializeQrPresentation().
   *
   * @param keyAlias Optional key alias to use (defaults to "testMdl")
   * @return Result with packId, credentialId, rawCredential, and keyAlias, or error
   */
  fun generateMockMdl(keyAlias: String?, callback: (Result<GenerateMockMdlResult>) -> Unit)

  companion object {
    /** The codec used by SpruceUtils. */
    val codec: MessageCodec<Any?> by lazy {
      SpruceUtilsPigeonCodec()
    }
    /** Sets up an instance of `SpruceUtils` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: SpruceUtils?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.SpruceUtils.generateMockMdl$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val keyAliasArg = args[0] as String?
            api.generateMockMdl(keyAliasArg) { result: Result<GenerateMockMdlResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(SpruceUtilsPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(SpruceUtilsPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
