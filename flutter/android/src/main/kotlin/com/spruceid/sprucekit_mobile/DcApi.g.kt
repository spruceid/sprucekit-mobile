// Autogenerated from Pigeon (v26.1.4), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.spruceid.sprucekit_mobile

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object DcApiPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is DcApiFlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a is ByteArray && b is ByteArray) {
        return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
        return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
        return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
        return a.contentEquals(b)
    }
    if (a is Array<*> && b is Array<*>) {
      return a.size == b.size &&
          a.indices.all{ deepEquals(a[it], b[it]) }
    }
    if (a is List<*> && b is List<*>) {
      return a.size == b.size &&
          a.indices.all{ deepEquals(a[it], b[it]) }
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      return a.size == b.size && a.all {
          (b as Map<Any?, Any?>).contains(it.key) &&
          deepEquals(it.value, b[it.key])
      }
    }
    return a == b
  }
      
}

/**
 * Error class for passing custom error details to Flutter via a thrown PlatformException.
 * @property code The error code.
 * @property message The error message.
 * @property details The error details. Must be a datatype supported by the api codec.
 */
class DcApiFlutterError (
  val code: String,
  override val message: String? = null,
  val details: Any? = null
) : Throwable()

/**
 * Result of DC API operations
 *
 * Generated class from Pigeon that represents data sent in messages.
 * This class should not be extended by any user class outside of the generated file.
 */
sealed class DcApiResult 
/**
 * Operation succeeded
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DcApiSuccess (
  val message: String? = null
) : DcApiResult()
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DcApiSuccess {
      val message = pigeonVar_list[0] as String?
      return DcApiSuccess(message)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      message,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other !is DcApiSuccess) {
      return false
    }
    if (this === other) {
      return true
    }
    return DcApiPigeonUtils.deepEquals(toList(), other.toList())  }

  override fun hashCode(): Int = toList().hashCode()
}

/**
 * Operation failed with error
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class DcApiError (
  val message: String
) : DcApiResult()
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): DcApiError {
      val message = pigeonVar_list[0] as String
      return DcApiError(message)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      message,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other !is DcApiError) {
      return false
    }
    if (this === other) {
      return true
    }
    return DcApiPigeonUtils.deepEquals(toList(), other.toList())  }

  override fun hashCode(): Int = toList().hashCode()
}

/**
 * Information about a registered credential
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class RegisteredCredentialInfo (
  /** Credential ID */
  val credentialId: String,
  /** Document type (e.g., "org.iso.18013.5.1.mDL") */
  val docType: String,
  /** Whether the credential is currently registered with the system */
  val isRegistered: Boolean
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): RegisteredCredentialInfo {
      val credentialId = pigeonVar_list[0] as String
      val docType = pigeonVar_list[1] as String
      val isRegistered = pigeonVar_list[2] as Boolean
      return RegisteredCredentialInfo(credentialId, docType, isRegistered)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      credentialId,
      docType,
      isRegistered,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other !is RegisteredCredentialInfo) {
      return false
    }
    if (this === other) {
      return true
    }
    return DcApiPigeonUtils.deepEquals(toList(), other.toList())  }

  override fun hashCode(): Int = toList().hashCode()
}
private open class DcApiPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DcApiSuccess.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          DcApiError.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          RegisteredCredentialInfo.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is DcApiSuccess -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is DcApiError -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is RegisteredCredentialInfo -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}


/**
 * Digital Credentials API
 *
 * Provides functionality for integrating with platform-specific Digital
 * Credentials APIs (iOS IdentityDocumentProvider, Android CredentialManager).
 *
 * This API handles:
 * - Syncing credentials to shared storage (App Groups) for extension access
 * - Registering/unregistering mDoc credentials with the platform
 *
 * Note: The actual DC API request handling is done by a native App Extension
 * (iOS) or Activity (Android) that reads credentials from shared storage.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface DcApi {
  /**
   * Sync credentials to App Group shared storage
   *
   * This makes credentials accessible to the DC API Extension which runs
   * in a separate process and cannot access the main app's storage.
   *
   * On iOS: Writes encrypted credential data to the App Group container
   * On Android: Not needed (CredentialManager handles this differently)
   *
   * @param appGroupId The App Group identifier (e.g., "group.com.example.app")
   * @param packIds List of credential pack IDs to sync
   * @return DcApiResult indicating success or error
   */
  fun syncCredentialsToAppGroup(appGroupId: String, packIds: List<String>, callback: (Result<DcApiResult>) -> Unit)
  /**
   * Register mDoc credentials with the platform's identity provider
   *
   * On iOS 26+: Registers with IdentityDocumentProviderRegistrationStore
   * On Android: Registers with CredentialManager registry
   *
   * This makes the credentials appear in the system's credential picker
   * when a website requests credentials via the DC API.
   *
   * @param packIds List of credential pack IDs containing mDocs to register
   * @param walletName Optional name to display for the wallet in credential selection UI
   * @return DcApiResult indicating success or error
   */
  fun registerCredentials(packIds: List<String>, walletName: String?, callback: (Result<DcApiResult>) -> Unit)
  /**
   * Unregister credentials from the platform's identity provider
   *
   * Removes the credentials from the system's credential picker.
   *
   * @param credentialIds List of credential IDs to unregister
   * @return DcApiResult indicating success or error
   */
  fun unregisterCredentials(credentialIds: List<String>, callback: (Result<DcApiResult>) -> Unit)
  /**
   * Get information about registered credentials
   *
   * @return List of registered credential info
   */
  fun getRegisteredCredentials(): List<RegisteredCredentialInfo>
  /**
   * Check if DC API is supported on this platform/version
   *
   * @return true if DC API is available (iOS 26+ or Android 14+)
   */
  fun isSupported(): Boolean

  companion object {
    /** The codec used by DcApi. */
    val codec: MessageCodec<Any?> by lazy {
      DcApiPigeonCodec()
    }
    /** Sets up an instance of `DcApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: DcApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.DcApi.syncCredentialsToAppGroup$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val appGroupIdArg = args[0] as String
            val packIdsArg = args[1] as List<String>
            api.syncCredentialsToAppGroup(appGroupIdArg, packIdsArg) { result: Result<DcApiResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(DcApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(DcApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.DcApi.registerCredentials$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val packIdsArg = args[0] as List<String>
            val walletNameArg = args[1] as String?
            api.registerCredentials(packIdsArg, walletNameArg) { result: Result<DcApiResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(DcApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(DcApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.DcApi.unregisterCredentials$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val credentialIdsArg = args[0] as List<String>
            api.unregisterCredentials(credentialIdsArg) { result: Result<DcApiResult> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(DcApiPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(DcApiPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.DcApi.getRegisteredCredentials$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.getRegisteredCredentials())
            } catch (exception: Throwable) {
              DcApiPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.sprucekit_mobile.DcApi.isSupported$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.isSupported())
            } catch (exception: Throwable) {
              DcApiPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
