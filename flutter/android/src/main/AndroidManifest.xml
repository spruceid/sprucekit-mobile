<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.spruceid.sprucekit_mobile">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- NFC tap-to-share (ISO 18013-5 device engagement over host card
         emulation). The feature is optional so the app still installs on
         phones without NFC. Those phones use QR engagement only. -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.nfc.hce"
        android:required="false" />

    <application>
        <!-- BIND_NFC_SERVICE limits binding to the Android NFC stack. -->
        <service
            android:name="com.spruceid.sprucekit_mobile.NfcPresentationService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/sprucekit_nfc_hce_service" />
        </service>
    </application>
</manifest>
