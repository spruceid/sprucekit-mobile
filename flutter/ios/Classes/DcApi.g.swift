// Autogenerated from Pigeon (v26.1.4), do not edit directly.
// See also: https://pub.dev/packages/pigeon

import Foundation

#if os(iOS)
  import Flutter
#elseif os(macOS)
  import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class DcApiPigeonError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "DcApiPigeonError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>")"
  }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  return [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? DcApiPigeonError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

private func isNullish(_ value: Any?) -> Bool {
  return value is NSNull || value == nil
}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}

func deepEqualsDcApi(_ lhs: Any?, _ rhs: Any?) -> Bool {
  let cleanLhs = nilOrValue(lhs) as Any?
  let cleanRhs = nilOrValue(rhs) as Any?
  switch (cleanLhs, cleanRhs) {
  case (nil, nil):
    return true

  case (nil, _), (_, nil):
    return false

  case is (Void, Void):
    return true

  case let (cleanLhsHashable, cleanRhsHashable) as (AnyHashable, AnyHashable):
    return cleanLhsHashable == cleanRhsHashable

  case let (cleanLhsArray, cleanRhsArray) as ([Any?], [Any?]):
    guard cleanLhsArray.count == cleanRhsArray.count else { return false }
    for (index, element) in cleanLhsArray.enumerated() {
      if !deepEqualsDcApi(element, cleanRhsArray[index]) {
        return false
      }
    }
    return true

  case let (cleanLhsDictionary, cleanRhsDictionary) as ([AnyHashable: Any?], [AnyHashable: Any?]):
    guard cleanLhsDictionary.count == cleanRhsDictionary.count else { return false }
    for (key, cleanLhsValue) in cleanLhsDictionary {
      guard cleanRhsDictionary.index(forKey: key) != nil else { return false }
      if !deepEqualsDcApi(cleanLhsValue, cleanRhsDictionary[key]!) {
        return false
      }
    }
    return true

  default:
    // Any other type shouldn't be able to be used with pigeon. File an issue if you find this to be untrue.
    return false
  }
}

func deepHashDcApi(value: Any?, hasher: inout Hasher) {
  if let valueList = value as? [AnyHashable] {
     for item in valueList { deepHashDcApi(value: item, hasher: &hasher) }
     return
  }

  if let valueDict = value as? [AnyHashable: AnyHashable] {
    for key in valueDict.keys { 
      hasher.combine(key)
      deepHashDcApi(value: valueDict[key]!, hasher: &hasher)
    }
    return
  }

  if let hashableValue = value as? AnyHashable {
    hasher.combine(hashableValue.hashValue)
  }

  return hasher.combine(String(describing: value))
}

    

/// Result of DC API operations
///
/// Generated class from Pigeon that represents data sent in messages.
/// This protocol should not be extended by any user class outside of the generated file.
protocol DcApiResult {

}

/// Operation succeeded
///
/// Generated class from Pigeon that represents data sent in messages.
struct DcApiSuccess: DcApiResult {
  var message: String? = nil


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> DcApiSuccess? {
    let message: String? = nilOrValue(pigeonVar_list[0])

    return DcApiSuccess(
      message: message
    )
  }
  func toList() -> [Any?] {
    return [
      message
    ]
  }
  static func == (lhs: DcApiSuccess, rhs: DcApiSuccess) -> Bool {
    return deepEqualsDcApi(lhs.toList(), rhs.toList())  }
  func hash(into hasher: inout Hasher) {
    deepHashDcApi(value: toList(), hasher: &hasher)
  }
}

/// Operation failed with error
///
/// Generated class from Pigeon that represents data sent in messages.
struct DcApiError: DcApiResult {
  var message: String


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> DcApiError? {
    let message = pigeonVar_list[0] as! String

    return DcApiError(
      message: message
    )
  }
  func toList() -> [Any?] {
    return [
      message
    ]
  }
  static func == (lhs: DcApiError, rhs: DcApiError) -> Bool {
    return deepEqualsDcApi(lhs.toList(), rhs.toList())  }
  func hash(into hasher: inout Hasher) {
    deepHashDcApi(value: toList(), hasher: &hasher)
  }
}

/// Information about a registered credential
///
/// Generated class from Pigeon that represents data sent in messages.
struct RegisteredCredentialInfo: Hashable {
  /// Credential ID
  var credentialId: String
  /// Document type (e.g., "org.iso.18013.5.1.mDL")
  var docType: String
  /// Whether the credential is currently registered with the system
  var isRegistered: Bool


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> RegisteredCredentialInfo? {
    let credentialId = pigeonVar_list[0] as! String
    let docType = pigeonVar_list[1] as! String
    let isRegistered = pigeonVar_list[2] as! Bool

    return RegisteredCredentialInfo(
      credentialId: credentialId,
      docType: docType,
      isRegistered: isRegistered
    )
  }
  func toList() -> [Any?] {
    return [
      credentialId,
      docType,
      isRegistered,
    ]
  }
  static func == (lhs: RegisteredCredentialInfo, rhs: RegisteredCredentialInfo) -> Bool {
    return deepEqualsDcApi(lhs.toList(), rhs.toList())  }
  func hash(into hasher: inout Hasher) {
    deepHashDcApi(value: toList(), hasher: &hasher)
  }
}

private class DcApiPigeonCodecReader: FlutterStandardReader {
  override func readValue(ofType type: UInt8) -> Any? {
    switch type {
    case 129:
      return DcApiSuccess.fromList(self.readValue() as! [Any?])
    case 130:
      return DcApiError.fromList(self.readValue() as! [Any?])
    case 131:
      return RegisteredCredentialInfo.fromList(self.readValue() as! [Any?])
    default:
      return super.readValue(ofType: type)
    }
  }
}

private class DcApiPigeonCodecWriter: FlutterStandardWriter {
  override func writeValue(_ value: Any) {
    if let value = value as? DcApiSuccess {
      super.writeByte(129)
      super.writeValue(value.toList())
    } else if let value = value as? DcApiError {
      super.writeByte(130)
      super.writeValue(value.toList())
    } else if let value = value as? RegisteredCredentialInfo {
      super.writeByte(131)
      super.writeValue(value.toList())
    } else {
      super.writeValue(value)
    }
  }
}

private class DcApiPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return DcApiPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return DcApiPigeonCodecWriter(data: data)
  }
}

class DcApiPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = DcApiPigeonCodec(readerWriter: DcApiPigeonCodecReaderWriter())
}


/// Digital Credentials API
///
/// Provides functionality for integrating with platform-specific Digital
/// Credentials APIs (iOS IdentityDocumentProvider, Android CredentialManager).
///
/// This API handles:
/// - Syncing credentials to shared storage (App Groups) for extension access
/// - Registering/unregistering mDoc credentials with the platform
///
/// Note: The actual DC API request handling is done by a native App Extension
/// (iOS) or Activity (Android) that reads credentials from shared storage.
///
/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol DcApi {
  /// Sync credentials to App Group shared storage
  ///
  /// This makes credentials accessible to the DC API Extension which runs
  /// in a separate process and cannot access the main app's storage.
  ///
  /// On iOS: Writes encrypted credential data to the App Group container
  /// On Android: Not needed (CredentialManager handles this differently)
  ///
  /// @param appGroupId The App Group identifier (e.g., "group.com.example.app")
  /// @param packIds List of credential pack IDs to sync
  /// @return DcApiResult indicating success or error
  func syncCredentialsToAppGroup(appGroupId: String, packIds: [String], completion: @escaping (Result<DcApiResult, Error>) -> Void)
  /// Register mDoc credentials with the platform's identity provider
  ///
  /// On iOS 26+: Registers with IdentityDocumentProviderRegistrationStore
  /// On Android: Registers with CredentialManager registry
  ///
  /// This makes the credentials appear in the system's credential picker
  /// when a website requests credentials via the DC API.
  ///
  /// @param packIds List of credential pack IDs containing mDocs to register
  /// @param walletName Optional name to display for the wallet in credential selection UI
  /// @return DcApiResult indicating success or error
  func registerCredentials(packIds: [String], walletName: String?, completion: @escaping (Result<DcApiResult, Error>) -> Void)
  /// Unregister credentials from the platform's identity provider
  ///
  /// Removes the credentials from the system's credential picker.
  ///
  /// @param credentialIds List of credential IDs to unregister
  /// @return DcApiResult indicating success or error
  func unregisterCredentials(credentialIds: [String], completion: @escaping (Result<DcApiResult, Error>) -> Void)
  /// Get information about registered credentials
  ///
  /// @return List of registered credential info
  func getRegisteredCredentials() throws -> [RegisteredCredentialInfo]
  /// Check if DC API is supported on this platform/version
  ///
  /// @return true if DC API is available (iOS 26+ or Android 14+)
  func isSupported() throws -> Bool
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class DcApiSetup {
  static var codec: FlutterStandardMessageCodec { DcApiPigeonCodec.shared }
  /// Sets up an instance of `DcApi` to handle messages through the `binaryMessenger`.
  static func setUp(binaryMessenger: FlutterBinaryMessenger, api: DcApi?, messageChannelSuffix: String = "") {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    /// Sync credentials to App Group shared storage
    ///
    /// This makes credentials accessible to the DC API Extension which runs
    /// in a separate process and cannot access the main app's storage.
    ///
    /// On iOS: Writes encrypted credential data to the App Group container
    /// On Android: Not needed (CredentialManager handles this differently)
    ///
    /// @param appGroupId The App Group identifier (e.g., "group.com.example.app")
    /// @param packIds List of credential pack IDs to sync
    /// @return DcApiResult indicating success or error
    let syncCredentialsToAppGroupChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sprucekit_mobile.DcApi.syncCredentialsToAppGroup\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      syncCredentialsToAppGroupChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let appGroupIdArg = args[0] as! String
        let packIdsArg = args[1] as! [String]
        api.syncCredentialsToAppGroup(appGroupId: appGroupIdArg, packIds: packIdsArg) { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      syncCredentialsToAppGroupChannel.setMessageHandler(nil)
    }
    /// Register mDoc credentials with the platform's identity provider
    ///
    /// On iOS 26+: Registers with IdentityDocumentProviderRegistrationStore
    /// On Android: Registers with CredentialManager registry
    ///
    /// This makes the credentials appear in the system's credential picker
    /// when a website requests credentials via the DC API.
    ///
    /// @param packIds List of credential pack IDs containing mDocs to register
    /// @param walletName Optional name to display for the wallet in credential selection UI
    /// @return DcApiResult indicating success or error
    let registerCredentialsChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sprucekit_mobile.DcApi.registerCredentials\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      registerCredentialsChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let packIdsArg = args[0] as! [String]
        let walletNameArg: String? = nilOrValue(args[1])
        api.registerCredentials(packIds: packIdsArg, walletName: walletNameArg) { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      registerCredentialsChannel.setMessageHandler(nil)
    }
    /// Unregister credentials from the platform's identity provider
    ///
    /// Removes the credentials from the system's credential picker.
    ///
    /// @param credentialIds List of credential IDs to unregister
    /// @return DcApiResult indicating success or error
    let unregisterCredentialsChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sprucekit_mobile.DcApi.unregisterCredentials\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      unregisterCredentialsChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let credentialIdsArg = args[0] as! [String]
        api.unregisterCredentials(credentialIds: credentialIdsArg) { result in
          switch result {
          case .success(let res):
            reply(wrapResult(res))
          case .failure(let error):
            reply(wrapError(error))
          }
        }
      }
    } else {
      unregisterCredentialsChannel.setMessageHandler(nil)
    }
    /// Get information about registered credentials
    ///
    /// @return List of registered credential info
    let getRegisteredCredentialsChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sprucekit_mobile.DcApi.getRegisteredCredentials\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      getRegisteredCredentialsChannel.setMessageHandler { _, reply in
        do {
          let result = try api.getRegisteredCredentials()
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      getRegisteredCredentialsChannel.setMessageHandler(nil)
    }
    /// Check if DC API is supported on this platform/version
    ///
    /// @return true if DC API is available (iOS 26+ or Android 14+)
    let isSupportedChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.sprucekit_mobile.DcApi.isSupported\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      isSupportedChannel.setMessageHandler { _, reply in
        do {
          let result = try api.isSupported()
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      isSupportedChannel.setMessageHandler(nil)
    }
  }
}
